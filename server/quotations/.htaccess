# Block direct HTTP access to stored PDFs (CyberPanel / Apache / LiteSpeed).
# Access files via File Manager, SFTP, or a future authenticated download script.
<IfModule authz_core_module>
  Require all denied
</IfModule>
